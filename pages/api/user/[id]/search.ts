import dbCon from '../../config/db'
import User from '../../db/User'
import Docs from '../../db/Docs'

//add search query to user's account with the PUT command
//remove search query from user's account with the DELETE command
